import { StubPage } from "@/components/StubPage";

export default function GeographyPage() {
  return (
    <StubPage
      title="Geography"
      breadcrumb="Dashboard / Geography"
      description="Chicago zip-code map with metric overlays ships in Phase C."
    />
  );
}
