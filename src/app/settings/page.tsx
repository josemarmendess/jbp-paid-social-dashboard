import { StubPage } from "@/components/StubPage";

export default function SettingsPage() {
  return (
    <StubPage
      title="Settings"
      breadcrumb="Dashboard / Settings"
      description="Workspace preferences, alerts, and team settings will live here."
    />
  );
}
