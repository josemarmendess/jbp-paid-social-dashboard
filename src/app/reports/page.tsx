import { StubPage } from "@/components/StubPage";

export default function ReportsPage() {
  return (
    <StubPage
      title="Reports"
      breadcrumb="Dashboard / Reports"
      description="PDF exports, share links, and email schedules are on the roadmap."
    />
  );
}
