import { StubPage } from "@/components/StubPage";

export default function PipelinePage() {
  return (
    <StubPage
      title="Pipeline"
      breadcrumb="Dashboard / Pipeline"
      description="Carryover, days-to-close, stale bookings, and cancellation trends ship in Phase B."
    />
  );
}
